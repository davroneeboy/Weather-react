# Weather React

Современное погодное приложение на React с TypeScript, использующее открытые API погоды.

## Возможности

- Поиск погоды по названию города
- Отображение текущей температуры и погодного состояния
- Детальная информация: влажность, давление, восход/закат, мин/макс температура, ветер, видимость
- Прогноз погоды на 7 дней (для Open-Meteo)
- Автоматическая смена фона в зависимости от температуры (теплый/холодный стиль)
- Современный UI с иконками и анимациями
- Адаптивный дизайн для всех устройств
- Поддержка трех API провайдеров (Open-Meteo, wttr.in, OpenWeatherMap)
- Информативный приветственный экран с описанием возможностей

## Установка и запуск

1. Склонируйте репозиторий:
   ```bash
   git clone <URL_вашего_репозитория>
   ```

2. Установите зависимости:
   ```bash
   npm install
   # или
   yarn install
   ```

3. Настройте API (см. раздел "Настройка API" ниже)

4. Запустите приложение:
   ```bash
   npm start
   # или
   yarn start
   ```

5. Откройте в браузере: [http://localhost:3000](http://localhost:3000)

## Настройка API

Приложение поддерживает три варианта API:

### Вариант 1: Open-Meteo (РЕКОМЕНДУЕТСЯ) 🌟

**Полностью бесплатно, без регистрации и API ключа!**

Создайте файл `.env` в корне проекта:
```env
REACT_APP_API_PROVIDER=openmeteo
```

Это все! Больше ничего настраивать не нужно. Включает прогноз на 7 дней.

### Вариант 2: wttr.in

**Бесплатно, без регистрации и API ключа!**

Создайте файл `.env` в корне проекта:
```env
REACT_APP_API_PROVIDER=wttr
```

Простое и быстрое API для получения погоды.

### Вариант 3: OpenWeatherMap

Требует бесплатный API ключ (получить можно на [openweathermap.org](https://openweathermap.org/api))

Создайте файл `.env` в корне проекта:
```env
REACT_APP_API_PROVIDER=openweather
REACT_APP_API_KEY=ваш_api_ключ_здесь
REACT_APP_API_BASE=https://api.openweathermap.org/data/2.5/
```

**Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен в репозиторий.

**По умолчанию:** Если не указан провайдер, приложение автоматически выберет Open-Meteo (если нет ключа) или OpenWeatherMap (если ключ указан).

## Используемые технологии

- **React 18** с TypeScript
- **Create React App**
- **React Icons** - библиотека иконок
- **Open-Meteo API**, **wttr.in API** или **OpenWeatherMap API**
- Современный CSS с градиентами и анимациями
- Адаптивный дизайн

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── search-box/     # Компонент поиска
│   ├── weather-display/# Отображение погоды
│   ├── loading/        # Индикатор загрузки
│   ├── error/          # Сообщения об ошибках
│   └── welcome/         # Приветственный экран
├── services/           # Сервисы для работы с API
├── types/              # TypeScript типы
├── utils/              # Утилиты
├── constants/          # Константы
└── App.tsx            # Главный компонент
```

## Особенности реализации

- ✅ Строгая типизация TypeScript
- ✅ Разделение на компоненты и сервисы
- ✅ Обработка ошибок
- ✅ Состояния загрузки
- ✅ Современный UI/UX дизайн
- ✅ Адаптивная верстка
- ✅ Оптимизация производительности

## Лицензия

MIT

---

### Автор
Weather React – современное погодное приложение на React с TypeScript.
